

  default: &default
      adapter: postgresql
      encoding: unicode
      pool: 5
      username: username
      password: password
      host:     <%= ENV['IP'] %>

development:
  <<: *default
  database: alphablog_development

test:
  <<: *default
  database: alphablog_test

production:
  <<: *default
  database: alphablog_production

